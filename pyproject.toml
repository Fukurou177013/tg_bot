[project]
name = "tg-bot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Цукер Богдан", email = "bogdan.tsuker@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
tg-bot = "tg_bot:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
